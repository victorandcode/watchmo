import React from 'react';
import SearchBar from '../../components/SearchBar/SearchBar';

const SearchBarContainer = () => (
  <div>
    <SearchBar onSubmit={this.performSearch} />
  </div>
);

export default SearchBarContainer;